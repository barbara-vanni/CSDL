#ifndef ERAZER_HPP
#define ERAZER_HPP
#include <raylib.h>
#include <iostream>
#include <vector>


    // void fillMatrix(std::vector<std::vector<int>>& matrix);
    void fillMatrix();
    void saveMatrixToFile(const std::string& filename);
    
#endif