#ifndef ALIVE_HPP
#define ALIVE_HPP

#include <vector> // Inclure l'en-tête pour std::vector

// Déclaration de la fonction alive
int alive(const std::vector<std::vector<int>>& matrix, int row, int col);

#endif

// #ifndef ALIVE_HPP
// #define ALIVE_HPP
// #include <raylib.h>
// #include <iostream>
// #include <vector>

// int alive(const std::vector<std::vector<int>>& matrix, int row, int col);

// #endif